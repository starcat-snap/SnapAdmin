/**
 * @package admin
 *
 * This is the initial start file for the whole administration. It loads
 * the SnapAdmin Core with the SnapAdmin object. And then starts to execute
 * the application.
 */

import 'src/core/snap-admin';
import 'src/app/main';
